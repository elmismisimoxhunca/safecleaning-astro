version: '3.8'

services:
  safecleaning:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: safecleaning-astro
    ports:
      - "4323:4323"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
